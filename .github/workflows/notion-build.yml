name: Notion → Netlify build trigger

permissions:
  contents: write


on:
  schedule:
    - cron: '*/10 * * * *' # every 10 minutes
  workflow_dispatch: {}     # allow manual runs from the Actions tab

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps
        run: npm i node-fetch dotenv

      - name: Run Notion poller
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          NETLIFY_BUILD_HOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}
        run: node scripts/notion-to-netlify.js

      # Persist state so subsequent runs know the last edit timestamp
      # inside your job, replace the push step
      - name: Commit last sync state
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f ".cache/notion-last-sync.json" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add .cache/notion-last-sync.json
            git commit -m "chore(ci): update Notion last-sync timestamp" || exit 0
            git push https://x-access-token:${GH_TOKEN}@github.com/wimmulder/life-in-weeks.git HEAD:main
          fi
